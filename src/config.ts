export const config = {
  defaultHeading: 90, // Default heading in degrees when device compass is unavailable
  distanceIncrement: 50, // Default distance increment in kilometers
  maxSearchAttempts: 50, // Maximum number of attempts to find a destination
  apiDelay: 1000, // Delay between API calls in milliseconds
  maxRetries: 3, // Maximum number of retries for failed API calls
  retryDelay: 2000, // Delay between retries in milliseconds
};